# Token compartido servidor ↔ clientes (cambiar en producción)
FRPS_TOKEN=genera-un-token-largo-aleatorio

# Puerto HTTP del vhost frp (Nginx Hestia hace proxy aquí; no usar 8080 = Apache)
FRPS_VHOST_PORT=18080

# Puerto control frp (solo interno / firewall)
FRPS_BIND_PORT=7000
