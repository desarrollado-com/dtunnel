# @desarrollado/dtunnel

CLI para exponer un servidor local como URL HTTPS pública.

Requiere **Node.js 18+**. Túnel nativo (WebSocket + gateway en la API).

```bash
npm install -g @desarrollado/dtunnel
dtunnel --port 3000
```

Documentación: https://dtunnel.desarrollado.com/docs.html
