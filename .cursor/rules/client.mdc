---
description: Convenciones del CLI dtunnel
globs: client/**
alwaysApply: false
---

# Client (CLI dtunnel)

- Túnel nativo: `native-client.js` + WebSocket a `/tunnel/ws`.
- Config en `~/.dtunnel/config.json` — no commitear tokens.
- Requiere Node.js 18+.
